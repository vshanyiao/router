apiVersion: v1
kind: Service
metadata:
  name: proxy
  namespace: maas
  labels:
    app: proxy
spec:
  type: ClusterIP
  selector:
    app: proxy
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
